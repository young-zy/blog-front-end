import { MarkdownSummaryPipe } from './markdown-summary.pipe';

describe('MarkdownSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkdownSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
